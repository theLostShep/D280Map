import { Routes } from '@angular/router';
import { MapComponent } from './map/map.component';

/************************PART D. USING ANGULAR ROUTING *****************/
export const routes: Routes = [
    { path: '', component: MapComponent }
];